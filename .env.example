# Copy to .env and fill with real values locally. Never commit .env.
# See .claude/docs/secrets-and-config.md

# Optional: local overrides (e.g. DynamoDB Local)
# AWS_REGION=us-east-1
# DYNAMODB_ENDPOINT=http://localhost:8000

# Placeholders only — real values stay in .env (gitignored) or Parameter Store
# AWS_ACCOUNT_ID=000000000000
# TABLE_SAVES=
# BUCKET_NOTES=
# API_BASE_URL=
