{
  "name": "@ai-learning-hub/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --coverage",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "@types/node": "^20.10.0",
    "esbuild": "^0.19.0",
    "typescript": "~5.3.0",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@clerk/backend": "^2.31.0"
  }
}
